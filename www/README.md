# Agentic First static site

This is the top-level umbrella site for:

```text
https://agentic-first.co/
```

It intentionally does not contain the Directory standard, schemas,
validator, examples, skills, or MCP service. Those now live in the
`pitch-mcp` repo and are exposed under `/directory/`.

`agent-first.co` is the memorable alternate domain and should redirect
to `agentic-first.co`.
